# pnpm 10+ fails an install outright when a dependency's build script is
# skipped. None of these postinstalls are needed for `next build`, and a new
# transitive dependency should not be able to break a deploy, so this downgrades
# it to a warning. The packages we do want built are listed in package.json.
strict-dep-builds=false
